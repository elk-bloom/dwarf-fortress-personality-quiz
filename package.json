{
  "name": "dwarf-fortress-personality-quiz",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "dwarf-frontend",
    "dwarf-backend",
    "shared"
  ],
  "scripts": {
    "format": "npm run format --workspaces",
    "build-backend": "npm run build -w dwarf-backend",
    "build-frontend": "npm run build -w dwarf-frontend",
    "build": "npm run build-backend && npm run build-frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.1.1",
    "tsc-alias": "^1.8.8",
    "typescript": "^5.3.3"
  }
}
